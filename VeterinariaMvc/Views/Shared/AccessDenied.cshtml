@{
    ViewData["Title"] = "Acceso denegado";
}

<div class="min-h-screen flex flex-col justify-center items-center bg-red-50 px-4">
    <div class="bg-white shadow-lg rounded-lg p-8 max-w-md text-center">
        <h1 class="text-4xl font-bold text-red-600 mb-4">🚫 Acceso denegado</h1>
        <p class="text-gray-700 mb-6">No tienes permisos para acceder a esta página.</p>
        <button type="button"
                onclick="history.back()"
                class="px-6 py-2 bg-red-600 text-white font-semibold rounded-lg shadow hover:bg-red-700 transition-colors duration-200">
            Volver atrás
        </button>
    </div>
</div>